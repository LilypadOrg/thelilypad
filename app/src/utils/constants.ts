import { ethers } from 'ethers';

export const SBT_MINT_FEE = ethers.utils.parseEther('0.25');
export const USERNAME_MIN_LENGTH = 3;
export const USERNAME_MAX_LENGTH = 42;
export const HOMEPAGE_COURSE_FILTERS = 20;
